export type CanteenItem = { id: string; name: string; description: string; category: string; pricePaise: number; imageUrl: string | null; options: string[]; status: 'AVAILABLE' | 'SOLD_OUT' | 'INACTIVE'; sortOrder: number }
export type CanteenLine = { id: string; itemId: string; itemName: string; unitPricePaise: number; quantity: number; customization: string | null; lineTotalPaise: number }
export type CanteenOrder = { id: string; referenceCode: string; status: string; paymentMethod: 'UPI' | 'CASH_ON_DELIVERY'; paymentStatus: string; upiReference: string | null; paymentScreenshotUrl: string | null; deliveryLocation: string; deliveryAt: string; notes: string | null; rejectionReason: string | null; totalPaise: number; createdAt: string; items: CanteenLine[]; requester: { id: string; name: string; email: string; department: { name: string } | null }; handler: { id: string; name: string } | null }
